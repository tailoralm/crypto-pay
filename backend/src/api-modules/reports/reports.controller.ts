export default class ReportsController {
    constructor(private userId: number) {
    }

    lastWeekGeneralReport() {
        // create a report with the general data of the last week
        // this data should be calculated by hour

        const report = [];
    }
}